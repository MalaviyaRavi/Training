<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <a class="navbar-brand" href="/"><b>Instagram</b></a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
      aria-controls="navbarNav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a
            class="nav-link <% if (active_page === 'index') { %> active <% } %>"
            href="/"
            >Feeds</a
          >
        </li>
        <% if (isLogin) { %>
        <li class="nav-item">
          <a
            class="
              nav-link
              <%
              if
              (active_page
              ===
              'profile')
              {
              %>
              active
              <%
              }
              %>
            "
            href="/profile"
            >Profile</a
          >
        </li>
        <% } %>
      </ul>
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <% if (!isLogin) { %>
        <li class="nav-item">
          <a
            class="nav-link <% if (active_page === 'login') { %> active <% } %>"
            href="/login"
            >Login</a
          >
        </li>
        <li class="nav-item">
          <a
            class="
              nav-link
              <%
              if
              (active_page
              ===
              'signup')
              {
              %>
              active
              <%
              }
              %>
            "
            href="/signup"
            >Signup</a
          >
        </li>

        <% }else { %>
        <li class="nav-item">
          <a
            class="
              nav-link
              <%
              if
              (active_page
              ===
              'logout')
              {
              %>
              active
              <%
              }
              %>
            "
            href="/logout"
            >Logout</a
          >
        </li>

        <% } %>
      </ul>
    </div>
  </div>
</nav>
